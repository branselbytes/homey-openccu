import { RuntimeBackedDriver } from "../../src/homey/runtime-backed-driver";

export = class HmIpPcbsBatDriver extends RuntimeBackedDriver {
  protected readonly openCcuDriverId = "HmIP-PCBS-BAT";
};
